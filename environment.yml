name: asi-ml
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - jupyterlab
  - ipykernel
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - pip
  - pip:
      - pre-commit
      - ruff
      - black
      - pytest
      - wandb
      - kedro
      - pyarrow
      - pandera
      - great-expectations
      - notebook
      - kedro-datasets
      - kedro-datasets[tracking]

  - pyyaml